function App() {
  const [activeTab, setActiveTab] = React.useState('chats'); // 'chats' or 'requests'
  const [selectedChat, setSelectedChat] = React.useState(null);
  const [newMessage, setNewMessage] = React.useState('');
  const [messages, setMessages] = React.useState({});

  const conversations = [
    {
      id: 1,
      name: 'Emma Wilson',
      avatar: 'EW',
      lastMessage: 'That LEGO set looks amazing! Is it still available?',
      time: '2m ago',
      unread: 2,
      online: true,
      toy: 'LEGO Creator Set'
    },
    {
      id: 2,
      name: 'Mike Rodriguez',
      avatar: 'MR',
      lastMessage: 'Perfect! When can we meet for the exchange?',
      time: '15m ago',
      unread: 0,
      online: true,
      toy: 'Spider-Man Figure'
    },
    {
      id: 3,
      name: 'Lisa Chen',
      avatar: 'LC',
      lastMessage: 'Thanks for the quick response!',
      time: '1h ago',
      unread: 1,
      online: false,
      toy: 'Learning Tablet'
    },
    {
      id: 4,
      name: 'David Park',
      avatar: 'DP',
      lastMessage: 'The soccer ball is in great condition',
      time: '3h ago',
      unread: 0,
      online: false,
      toy: 'Soccer Ball'
    },
    {
      id: 5,
      name: 'Anna Thompson',
      avatar: 'AT',
      lastMessage: 'I\'d love to see more photos if possible',
      time: '1d ago',
      unread: 0,
      online: true,
      toy: 'Puzzle Set'
    }
  ];

  const exchangeRequests = [
    {
      id: 1,
      name: 'Sarah Miller',
      avatar: 'SM',
      toy: 'LEGO Creator 3-in-1 Deep Sea Creatures',
      requestedToy: 'Your Marvel Action Figure',
      time: '30m ago',
      status: 'pending',
      message: 'Hi! I\'d love to exchange my LEGO set for your Marvel figure. My daughter would love it!'
    },
    {
      id: 2,
      name: 'John Davis',
      avatar: 'JD',
      toy: 'Educational Tablet',
      requestedToy: 'Your Building Blocks',
      time: '2h ago',
      status: 'pending',
      message: 'Would you be interested in trading? The tablet is in excellent condition.'
    },
    {
      id: 3,
      name: 'Maria Garcia',
      avatar: 'MG',
      toy: 'Barbie Dreamhouse',
      requestedToy: 'Your Art Supplies',
      time: '1d ago',
      status: 'accepted',
      message: 'Great! Looking forward to the exchange.'
    }
  ];

  const chatMessages = {
    1: [
      {
        id: 1,
        sender: 'them',
        message: 'Hi! I saw your LEGO Creator set listing',
        time: '10:30 AM',
        avatar: 'EW'
      },
      {
        id: 2,
        sender: 'me',
        message: 'Hello! Yes, it\'s still available. Are you interested?',
        time: '10:32 AM'
      },
      {
        id: 3,
        sender: 'them',
        message: 'That LEGO set looks amazing! Is it still available?',
        time: '10:35 AM',
        avatar: 'EW'
      },
      {
        id: 4,
        sender: 'them',
        message: 'My son would absolutely love it!',
        time: '10:35 AM',
        avatar: 'EW'
      },
      {
        id: 5,
        sender: 'me',
        message: 'That\'s wonderful! It\'s perfect for creative kids. Would you like to arrange an exchange?',
        time: '10:38 AM'
      }
    ]
  };

  const sendMessage = () => {
    if (newMessage.trim() && selectedChat) {
      const newMsg = {
        id: Date.now(),
        sender: 'me',
        message: newMessage,
        time: new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })
      };
      
      setMessages(prev => ({
        ...prev,
        [selectedChat]: [...(prev[selectedChat] || chatMessages[selectedChat] || []), newMsg]
      }));
      
      setNewMessage('');
    }
  };

  const handleRequest = (requestId, action) => {
    // Handle accept/decline request logic
    console.log(`${action} request ${requestId}`);
  };

  if (selectedChat) {
    const conversation = conversations.find(c => c.id === selectedChat);
    const currentMessages = messages[selectedChat] || chatMessages[selectedChat] || [];

    return (
      <div className="min-h-screen bg-gradient-to-br from-purple-50 to-pink-50 flex flex-col">
        {/* Chat Header */}
        <div className="bg-white shadow-sm border-b border-gray-100 px-4 py-4">
          <div className="flex items-center space-x-3">
            <button 
              onClick={() => setSelectedChat(null)}
              className="w-8 h-8 bg-gray-100 rounded-lg flex items-center justify-center"
            >
              <i className="fas fa-arrow-left text-gray-600"></i>
            </button>
            <div className="relative">
              <div className="w-10 h-10 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center">
                <span className="text-white text-sm font-bold">{conversation.avatar}</span>
              </div>
              {conversation.online && (
                <div className="absolute -bottom-1 -right-1 w-3 h-3 bg-green-500 rounded-full border-2 border-white"></div>
              )}
            </div>
            <div className="flex-1">
              <h2 className="font-semibold text-gray-800">{conversation.name}</h2>
              <p className="text-sm text-gray-500">
                {conversation.online ? 'Online' : 'Last seen 2h ago'}
              </p>
            </div>
            <div className="flex items-center space-x-2">
              <button className="w-8 h-8 bg-gray-100 rounded-lg flex items-center justify-center">
                <i className="fas fa-phone text-gray-600 text-sm"></i>
              </button>
              <button className="w-8 h-8 bg-gray-100 rounded-lg flex items-center justify-center">
                <i className="fas fa-ellipsis-v text-gray-600 text-sm"></i>
              </button>
            </div>
          </div>
        </div>

        {/* Toy Context Bar */}
        <div className="bg-purple-100 border-b border-purple-200 px-4 py-3">
          <div className="flex items-center space-x-3">
            <div className="w-8 h-8 bg-purple-500 rounded-lg flex items-center justify-center">
              <i className="fas fa-cube text-white text-sm"></i>
            </div>
            <div className="flex-1">
              <p className="text-sm font-medium text-purple-800">Discussing: {conversation.toy}</p>
              <p className="text-xs text-purple-600">Tap to view toy details</p>
            </div>
            <button className="text-purple-600">
              <i className="fas fa-external-link-alt text-sm"></i>
            </button>
          </div>
        </div>

        {/* Messages */}
        <div className="flex-1 px-4 py-4 space-y-4 overflow-y-auto">
          {currentMessages.map((msg) => (
            <div
              key={msg.id}
              className={`flex ${msg.sender === 'me' ? 'justify-end' : 'justify-start'}`}
            >
              <div className={`flex items-end space-x-2 max-w-xs ${msg.sender === 'me' ? 'flex-row-reverse space-x-reverse' : ''}`}>
                {msg.sender === 'them' && (
                  <div className="w-8 h-8 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center flex-shrink-0">
                    <span className="text-white text-xs font-bold">{msg.avatar}</span>
                  </div>
                )}
                <div className="flex flex-col">
                  <div
                    className={`px-4 py-3 rounded-2xl ${
                      msg.sender === 'me'
                        ? 'bg-gradient-to-r from-purple-500 to-pink-500 text-white rounded-br-md'
                        : 'bg-white text-gray-800 rounded-bl-md shadow-sm border border-gray-100'
                    }`}
                  >
                    <p className="text-sm">{msg.message}</p>
                  </div>
                  <p className={`text-xs text-gray-500 mt-1 ${msg.sender === 'me' ? 'text-right' : 'text-left'}`}>
                    {msg.time}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Message Input */}
        <div className="bg-white border-t border-gray-200 px-4 py-4">
          <div className="flex items-center space-x-3">
            <button className="w-10 h-10 bg-gray-100 rounded-full flex items-center justify-center">
              <i className="fas fa-plus text-gray-600"></i>
            </button>
            <div className="flex-1 relative">
              <input
                type="text"
                value={newMessage}
                onChange={(e) => setNewMessage(e.target.value)}
                onKeyPress={(e) => e.key === 'Enter' && sendMessage()}
                placeholder="Type a message..."
                className="w-full px-4 py-3 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-purple-500 focus:bg-white transition-all"
              />
              <button className="absolute right-2 top-1/2 transform -translate-y-1/2 w-8 h-8 bg-gray-200 rounded-full flex items-center justify-center">
                <i className="fas fa-smile text-gray-600 text-sm"></i>
              </button>
            </div>
            <button
              onClick={sendMessage}
              className="w-10 h-10 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center"
            >
              <i className="fas fa-paper-plane text-white text-sm"></i>
            </button>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-50 to-pink-50">
      {/* Header */}
      <div className="bg-white shadow-sm border-b border-gray-100">
        <div className="px-4 py-4">
          <div className="flex items-center justify-between mb-4">
            <h1 className="text-xl font-bold text-gray-800">Messages</h1>
            <button className="w-8 h-8 bg-gray-100 rounded-lg flex items-center justify-center">
              <i className="fas fa-edit text-gray-600"></i>
            </button>
          </div>

          {/* Tabs */}
          <div className="flex space-x-1 bg-gray-100 rounded-xl p-1">
            <button
              onClick={() => setActiveTab('chats')}
              className={`flex-1 py-2 px-4 rounded-lg text-sm font-medium transition-all ${
                activeTab === 'chats'
                  ? 'bg-white text-gray-800 shadow-sm'
                  : 'text-gray-500 hover:text-gray-700'
              }`}
            >
              Chats
            </button>
            <button
              onClick={() => setActiveTab('requests')}
              className={`flex-1 py-2 px-4 rounded-lg text-sm font-medium transition-all ${
                activeTab === 'requests'
                  ? 'bg-white text-gray-800 shadow-sm'
                  : 'text-gray-500 hover:text-gray-700'
              }`}
            >
              Requests
            </button>
          </div>
        </div>
      </div>

      {/* Content */}
      <div className="px-4 py-4">
        {activeTab === 'chats' ? (
          /* Chat List */
          <div className="space-y-2">
            {conversations.map((conversation) => (
              <button
                key={conversation.id}
                onClick={() => setSelectedChat(conversation.id)}
                className="w-full bg-white rounded-2xl shadow-sm border border-gray-100 p-4 hover:shadow-md transition-all text-left"
              >
                <div className="flex items-center space-x-3">
                  <div className="relative">
                    <div className="w-12 h-12 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center">
                      <span className="text-white font-bold">{conversation.avatar}</span>
                    </div>
                    {conversation.online && (
                      <div className="absolute -bottom-1 -right-1 w-4 h-4 bg-green-500 rounded-full border-2 border-white"></div>
                    )}
                  </div>
                  <div className="flex-1 min-w-0">
                    <div className="flex items-center justify-between mb-1">
                      <h3 className="font-semibold text-gray-800 truncate">{conversation.name}</h3>
                      <div className="flex items-center space-x-2">
                        <span className="text-xs text-gray-500">{conversation.time}</span>
                        {conversation.unread > 0 && (
                          <div className="w-5 h-5 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center">
                            <span className="text-white text-xs font-bold">{conversation.unread}</span>
                          </div>
                        )}
                      </div>
                    </div>
                    <p className="text-sm text-gray-500 truncate mb-1">{conversation.lastMessage}</p>
                    <div className="flex items-center space-x-1">
                      <i className="fas fa-cube text-purple-500 text-xs"></i>
                      <span className="text-xs text-purple-600">{conversation.toy}</span>
                    </div>
                  </div>
                </div>
              </button>
            ))}
          </div>
        ) : (
          /* Exchange Requests */
          <div className="space-y-4">
            {exchangeRequests.map((request) => (
              <div
                key={request.id}
                className="bg-white rounded-2xl shadow-sm border border-gray-100 p-4"
              >
                <div className="flex items-start space-x-3 mb-4">
                  <div className="w-12 h-12 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center">
                    <span className="text-white font-bold">{request.avatar}</span>
                  </div>
                  <div className="flex-1">
                    <div className="flex items-center justify-between mb-2">
                      <h3 className="font-semibold text-gray-800">{request.name}</h3>
                      <span className="text-xs text-gray-500">{request.time}</span>
                    </div>
                    <div className="bg-gray-50 rounded-xl p-3 mb-3">
                      <div className="flex items-center justify-between text-sm">
                        <div className="flex-1">
                          <div className="font-medium text-gray-800 mb-1">Offering:</div>
                          <div className="text-gray-600">{request.toy}</div>
                        </div>
                        <div className="mx-3">
                          <i className="fas fa-exchange-alt text-purple-500"></i>
                        </div>
                        <div className="flex-1">
                          <div className="font-medium text-gray-800 mb-1">Wants:</div>
                          <div className="text-gray-600">{request.requestedToy}</div>
                        </div>
                      </div>
                    </div>
                    <p className="text-sm text-gray-600 mb-4">{request.message}</p>
                    
                    {request.status === 'pending' ? (
                      <div className="flex space-x-3">
                        <button
                          onClick={() => handleRequest(request.id, 'decline')}
                          className="flex-1 bg-gray-100 text-gray-700 py-2 rounded-xl text-sm font-medium hover:bg-gray-200 transition-colors"
                        >
                          Decline
                        </button>
                        <button
                          onClick={() => handleRequest(request.id, 'accept')}
                          className="flex-1 bg-gradient-to-r from-purple-500 to-pink-500 text-white py-2 rounded-xl text-sm font-medium hover:shadow-lg transition-all"
                        >
                          Accept
                        </button>
                      </div>
                    ) : (
                      <div className="flex items-center space-x-2">
                        <i className="fas fa-check-circle text-green-500"></i>
                        <span className="text-sm font-medium text-green-600">Request Accepted</span>
                      </div>
                    )}
                  </div>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>

      {/* Bottom Navigation */}
      <div className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 px-4 py-2">
        <div className="flex items-center justify-around">
          <button className="flex flex-col items-center space-y-1 py-2">
            <div className="w-6 h-6 bg-gray-100 rounded-lg flex items-center justify-center">
              <i className="fas fa-home text-gray-400 text-sm"></i>
            </div>
            <span className="text-xs text-gray-400">Home</span>
          </button>
          
          <button className="flex flex-col items-center space-y-1 py-2">
            <div className="w-6 h-6 bg-gray-100 rounded-lg flex items-center justify-center">
              <i className="fas fa-search text-gray-400 text-sm"></i>
            </div>
            <span className="text-xs text-gray-400">Browse</span>
          </button>
          
          <button className="flex flex-col items-center space-y-1 py-2">
            <div className="w-8 h-8 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center shadow-lg">
              <i className="fas fa-plus text-white text-sm"></i>
            </div>
          </button>
          
          <button className="flex flex-col items-center space-y-1 py-2">
            <div className="w-6 h-6 bg-gradient-to-r from-purple-500 to-pink-500 rounded-lg flex items-center justify-center">
              <i className="fas fa-comment text-white text-sm"></i>
            </div>
            <span className="text-xs font-medium text-purple-500">Messages</span>
          </button>
          
          <button className="flex flex-col items-center space-y-1 py-2">
            <div className="w-6 h-6 bg-gray-100 rounded-lg flex items-center justify-center">
              <i className="fas fa-user text-gray-400 text-sm"></i>
            </div>
            <span className="text-xs text-gray-400">Profile</span>
          </button>
        </div>
      </div>

      {/* Bottom Padding */}
      <div className="h-20"></div>
    </div>
  );
}